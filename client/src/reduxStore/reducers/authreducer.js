const initState = {
    user:null,
    accessToken:null,
    refereshToken:null,
    errorMessage:null,
    successMessage:null
}

const authReducer =(state = initState,action)=>{
    const{type,payload} = action

}